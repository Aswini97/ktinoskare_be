# Basic MQTT Broker Configuration
listener 1883
allow_anonymous true

# Data Persistence (so data isn't lost on restart)
persistence true
persistence_location /mosquitto/data/

# Logging for debugging cattle collar pings
log_dest stdout
log_type all